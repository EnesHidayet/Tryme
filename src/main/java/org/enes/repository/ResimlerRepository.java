package org.enes.repository;

import org.enes.entity.Resimler;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResimlerRepository extends JpaRepository<Resimler, Long> {
}
